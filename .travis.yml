language: php

php:
  - 8.2
  - 8.3

matrix:
  allow_failures:
    - php: 8.3

install:
  - composer selfupdate
  - composer install

script:
  - ./vendor/bin/phpcbf
  - ./vendor/bin/phpunit --colors --coverage-text
